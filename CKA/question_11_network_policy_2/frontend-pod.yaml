apiVersion: v1
kind: Pod
metadata:
  name: frontend-app
  namespace: project-c
  labels:
    tier: frontend
spec:
  containers:
  - name: frontend-app-container
    image: nginx:latest
    ports:
    - containerPort: 80
